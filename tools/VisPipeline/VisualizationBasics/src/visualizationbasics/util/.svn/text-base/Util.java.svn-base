/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package visualizationbasics.util;

/**
 *
 * @author Fernando V. Paulovic
 */
public class Util {

    public static boolean isParsableToInt(String str) {
        try {
            Integer.parseInt(str);
            return true;
        } catch (NumberFormatException nfe) {
            return false;
        }

//        char[] chars = str.toCharArray();
//        if ((chars[0] == '-' && str.length() > 1) || Character.isDigit(chars[0])) {
//            for (int i = 1; i < chars.length; i++) {
//                if (!Character.isDigit(chars[i])) {
//                    return false;
//                }
//            }
//        } else {
//            return false;
//        }

//        return (str.length() > 0);
    }

    public static int convertToInt(String string) {
        return string.toLowerCase().hashCode();
    }
}
